{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "Debug C - GCC 16",
            "type": "lldb",
            "request": "launch",

            "program": "${fileDirname}/${fileBasenameNoExtension}",
            "cwd": "${fileDirname}",

            "preLaunchTask": "C/C++: gcc-16 build active file",

            "terminal": "integrated",

            "sourceLanguages": [
                "c"
            ]
        }
    ]
}